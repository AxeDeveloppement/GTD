> **INSTRUCTION D'INITIALISATION (POUR L'AGENT IA)**
> Si ce document contient la variable `{{project_id}}`, tu te trouves dans un nouveau projet non initialisé. Ton TOUT PREMIER objectif, avant de répondre à toute autre requête, est de :
> 1. Demander à l'utilisateur : *"Quel est le nom de ce nouveau projet ?"*
> 2. Déduire le chemin absolu du dossier de travail actuel (source_path).
> 3. Créer un nouveau fichier nommé `.clinerules.md`.
> 4. Copier l'intégralité des directives ci-dessous dans ce nouveau fichier, en remplaçant toutes les occurrences de `{{project_id}}` par le nom fourni, et `{{source_path}}` par le chemin du dossier.
> 5. Confirmer que la configuration est terminée et poursuivre la tâche demandée.

---

# Directives du Projet {{project_id}}

## 1. Contexte du Projet & Infrastructure
*   **Nom du projet (project_id) :** {{project_id}}
*   **Chemin racine (source_path) :** {{source_path}}
*   **Serveur MCP (ai-engine) :** Centralisé sur http://100.77.221.66:8001 via SSE[cite: 4].

**Modèles LLM hébergés (Serveur Ollama) :**
*   **GPU 1 (100.77.221.66:11434) :** llama3.2:3b (résumés), qwen2.5vl:7b-q4_K_M (vision UI), nomic-embed-text (embeddings), qwen3.6:27b[cite: 4].
*   **GPU 0 (100.77.221.66:11435) :** deepseek-r1:14b (revues de code approfondies)[cite: 4].

## 2. Architecture & Rôles
**Séparation UI / Host (STRICT) :**
*   `{{project_id}}.UI.Shared` : Reçoit TOUS les composants Blazor, pages et layouts[cite: 4].
*   `{{project_id}}.WebApplication` (.Web) : Rôle d'hôte uniquement (Program.cs, middleware, DI). Aucun composant UI ne doit s'y trouver[cite: 4].

## 3. Déroulement du Travail & Validation
*   **Context Loading :** Si tu manques de visibilité sur le projet lors d'une tâche complexe, interroge le serveur MCP (`get_project_context` / `search_codebase`)[cite: 4].
*   **Revue de Code :** Fais valider les changements d'architecture critiques via `ask_code_reviewer`. Invoque de ta propre initiative `ask_code_reviewer` chaque fois que tu modifies une logique de sécurité, que tu altères l'architecture centrale du projet, ou que tu dois résoudre un bug critique[cite: 4].
*   **Traçabilité (ADR) :** Dès qu'une décision d'architecture est validée, consigne-la via `log_architecture_decision`[cite: 4].
*   **Style :** Sois direct, concret et réponds en français[cite: 4].

## 4. Format d'Appel des Outils MCP (ai-engine)
Lors des appels d'outils MCP, tu DOIS fournir un objet JSON natif[cite: 4].

### REGLES STRICTES ANTI-ERREUR -32602 :
1. **PAS DE GUILLEMETS ÉCHAPPÉS DANS LES VALEURS** : Ne mets JAMAIS de `\"` à l'intérieur d'une valeur string[cite: 4].
   - CORRECT : `"project_id": "{{project_id}}"`[cite: 4]
   - INCORRECT : `"project_id": "\"{{project_id}}\""`[cite: 4]
2. **PAS DE STRINGIFICATION** : L'argument global doit être un objet JSON natif, pas un bloc de texte encodé en chaîne[cite: 4].

### Exemples exacts (à copier à la lettre) :
*   `get_project_context` : `{"project_id": "{{project_id}}"}`
*   `save_session_summary` : `{"project_id": "{{project_id}}", "summary": "Résumé de la session", "tasks_completed": ["Tâche 1"], "next_steps": ["Prochaine étape"]}`
*   `log_architecture_decision` : `{"project_id": "{{project_id}}", "topic": "Sujet", "decision": "Décision", "rationale": "Raison"}`
*   `trigger_codebase_indexing` : `{"project_id": "{{project_id}}", "source_path": "{{source_path}}", "clean_first": true}`
*   `search_codebase` : `{"project_id": "{{project_id}}", "query": "Terme recherché", "top_k": 5}`
*   `ask_code_reviewer` (Cas nominal par fichier) : `{"file_path": "{{source_path}}\\Components\\Bouton.razor", "focus_area": "security"}`
*   `ask_code_reviewer` (Cas exceptionnel en mémoire) : `{"code_snippet": "public void Test() { }", "focus_area": "general"}`
*   `ask_vision_expert` : `{"image_path": "{{source_path}}\\docs\\ui_mockup.png", "user_prompt": "Analyse visuelle"}`
*   `generate_auto_summary` : `{"chat_log": "Texte de la discussion"}`